import Select from "./Select";

function Header() {
  return (
    <header className="App-header">
      <Select type="source" />
      <Select type="category" />
    </header>
  );
}

export default Header;
